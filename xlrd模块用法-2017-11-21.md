打开excel表格
```
excel_obj = xlrd.open_workbook("oa_new.xlsx")
```
选择工作表
```
# 按序号选择
table = excel_obj.sheets()[0]
table = excel_obj.sheet_by_index(0)

# 按工作表名选择
excel_obj.sheet_by_name("请假出差信息".decode("utf8"))



table.nrows  行数
table.ncols  列数
```